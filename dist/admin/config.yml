backend:
  name: github
  repo: polymathnetwork/polymath-website
  branch: dev # Branch to update (optional; defaults to master)
# This line should *not* be indented
media_folder: "assets/images/team"
# public_folder: "/images"
collections:
  - label: "Team"
    name: "team"
    folder: "assets/images/team" # The path to the folder where the documents are stored
    widget: "list"
    create: true
    fields:
      - {
          label: Quote,
          name: quote,
          widget: string,
          default: "Everything is awesome!",
        }
      - label: Author
        name: author
        widget: object
        fields:
          - { label: Name, name: name, widget: string, default: "Emmet" }
          - {
              label: Avatar,
              name: avatar,
              widget: image,
              default: "/img/emmet.jpg",
            }
